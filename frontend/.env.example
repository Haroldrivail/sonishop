# Frontend environment variables
# When developing locally with `npm run dev`, Vite will proxy /api -> http://localhost:8000
# To point the built frontend to an API server in production, set:
VITE_API_URL=https://api.sonishop.test
