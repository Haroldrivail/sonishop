<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Frontend URL
    |--------------------------------------------------------------------------
    |
    | The full URL where the frontend application is hosted. Controllers
    | that previously rendered Inertia pages will redirect the browser to
    | this URL so the SPA handles the UI.
    |
    */
    'url' => env('FRONTEND_URL', 'http://localhost:5175'),
];
